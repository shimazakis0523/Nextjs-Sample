# 画面定義書: [業務行為のトピック名]

**画面ID**: `[SCREEN_ID]`

**対象URL**: `[URL、モーダル等で独立URLを持たない場合は省略し代わりに以下を使う]`

**表示形式**: [独立したURLを持たない場合のみ。例: モーダル。独立したURLは持たない。]

**作成日**: [DATE]

**関連**: [ユースケース記述([業務行為のトピック名])](./ユースケース記述_[画面名].md)

<!--
  ACTION REQUIRED:
  - ファイル名は `画面定義書_<画面名>.md`。<画面名>には連番を付けない(ADR-0015)。
  - 「対象URL」と「表示形式」はどちらか一方のみ書く(独立URLを持つ画面は対象URL、
    モーダル等は表示形式)。
  - 自分がどの画面から起動されるか(遷移元)は書かないこと。自分自身が持つボタン・
    リンクがどこへ導くか(遷移先)は「処理仕様」に書くこと。
  - エンティティ/データモデルの定義はここに書かない。それはサーバーサイドの関心事。
-->

## 画面入出力仕様

<!--
  IMPORTANT:
  - 画面に映る全要素をここに列挙する。入力項目だけでなく、ボタン・見出し・
    バッジ・メッセージ表示欄など「出力」される要素も全てここに含める。
  - 項目単位の見た目・制約に関する情報は、必ずその項目の行の中に記載する。
    表の外に補足コメントを書いてはならない。
  - 行の順序は画面上の配置順(上から下、左から右)に合わせる。
  - 入力項目には、未入力時に表示されるヒント文言(placeholder)の有無を必ず記載する。
    無い場合も「なし」と明記し、欄を空にしない(記述漏れと区別できるようにする)。
    入力でない項目(ボタン・見出し等)は「—」でよい。
  - 「入出力区分」と「種別」は別の列にする。種別欄に「出力(静的テキスト)」のように
    区分を括弧書きで詰め込まない。同様に、一覧テーブルの行ごとに繰り返される項目
    である、といった種別以外の付帯情報も種別欄に括弧書きせず、備考欄に書く。

  ACTION REQUIRED: 以下は記入例(サンプル)。実際の項目に置き換えること。
-->

| # | 項目名 | 入出力区分 | 種別 | 表示内容・初期値 | プレースホルダ | 制約 | 備考 |
|---|---|---|---|---|---|---|---|
| 1 | [項目名] | [入力 / 表示 / 操作] | [自由入力テキスト / 日付ピッカー / ドロップダウン / 静的テキスト / 条件表示テキスト / バッジ / ボタン / 確認ダイアログ等] | [初期表示時に何が表示されるか] | [ヒント文言、または「なし」。入力でない項目は「—」] | [必須/任意、文字種、桁数、許容値など] | [その項目固有の補足があれば] |

## 処理仕様

<!--
  IMPORTANT:
  - 発火契機(初期表示、またはボタンクリック等の操作)ごとに1行で処理内容を書く。
  - 初期表示は必ず1行目に書く。それ以外の順序は、画面上の操作対象(ボタン等)の
    並び順に合わせることを推奨する。
  - この仕様書はフロントエンドの範囲に閉じる。バックエンドAPIを呼び出す処理が
    ある場合、呼び出すエンドポイント(メソッド+パス。例: `POST /api/todos`)は
    契約として明記する(`doc/API仕様書/BFF/openapi.yaml`と一致させる)。ただし、その
    エンドポイントの詳細な仕様(リクエスト/レスポンスの形、ステータスコード、
    DBへの永続化方法等)はここに再掲せず、`doc/API仕様書/BFF/openapi.yaml`への参照に
    留める。フロントエンド側から見た「成功した場合は〜、失敗した場合は〜」の
    分岐は書く。
  - この画面自身のボタン・リンクが他画面へ遷移する場合、その行で遷移先画面の
    ユースケース記述へのMarkdownリンク(`[画面名](./ユースケース記述_<画面名>.md)`)を
    含めること — update-screen-flow-diagramスキルがこのリンクから画面遷移図.mdを
    機械的に生成する。

  ACTION REQUIRED: 以下はプレースホルダ。実際の処理仕様に置き換えること。
-->

| # | 発火契機 | 処理内容 |
|---|---|---|
| 1 | 初期表示 | [画面が最初に表示される際の処理] |
| 2 | [操作(例: 〜ボタンをクリック)] | [その操作で起きる処理。成功/失敗で分岐する場合はそれぞれ記載] |
