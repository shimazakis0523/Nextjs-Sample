# [業務行為/画面のトピック名]

**画面ID**: `[SCREEN_ID]`

**モックアップ**: [Artifact URL]

**作成日**: [DATE]

<!--
  ACTION REQUIRED:
  - タイトルは画面名ではなく、この画面が実現する業務行為の名前にすること
    (例: 「Todo一覧画面」ではなく「Todo一覧」、「Todo新規登録画面」ではなく「Todo新規登録」)。
  - この画面が独立したURLを持つか、モーダル等かを明記すること。
  - 自分がどの画面から起動されるか(遷移元)は書かないこと。自分自身が持つボタン・
    リンクがどこへ導くか(遷移先)は「処理仕様」に書くこと。
  - エンティティ/データモデルの定義はここに書かない。それはサーバーサイドの関心事。
  - 「親機能」のような、この文書がどのディレクトリに属するかの自己言及は書かない
    (ファイルパス自体が既に示している)。
  - 「ステータス: Draft」のような、この文書自体の進捗・ワークフロー状態は書かない
    (設計内容ではなく文書管理の話であり、レビュー状態はPR等で管理する)。
  - 「モックアップ」は、この画面のユースケース定義・画面入出力仕様を起こす元になった
    合意済みモックアップ(designスキルで作成したArtifact)のURLを書く。これは
    openapi.yamlへの参照とは性質が異なるので注意: openapi.yamlは常に現在の契約と
    一致していなければならない「現在の一次情報源」だが、モックアップは執筆時点の
    視覚的合意の記録に過ぎない。画面入出力仕様・処理仕様は必ずこのテーブル内で
    自己完結させ、「詳細はモックアップ参照」のように内容を書かずに済ませることは
    禁止。spec.mdの記述がモックアップと食い違った場合は常にspec.mdが正であり、
    モックアップを都度更新し直す必要はない。モックアップが無い場合は行ごと省略する。
-->

## 目次

1. [ユースケース定義](#ユースケース定義)
2. [画面定義](#画面定義)

## ユースケース定義

### ユースケース記述

<!--
  IMPORTANT: ここはユーザーストーリー(優先度・独立テスト可否などアジャイル的な情報)を
  書く場所ではない。1つのユースケースにつき、アクター・事前条件・基本フロー・
  代替/例外フロー・事後条件のみを書く。この画面が複数のユースケースを持つ場合は
  UC-01, UC-02 ... を並べる。

  ACTION REQUIRED: 以下はプレースホルダ。実際のユースケースに置き換えること。
-->

#### UC-01: [ユースケース名]

**アクター**: [この操作を行う人]

**事前条件**: [この操作を開始できる前提条件]

**基本フロー**:

1. [手順]
2. [手順]

**代替/例外フロー**:

- [条件]: [その場合の結果]

**事後条件**: [完了後の状態]

## 画面定義

### 画面入出力仕様

<!--
  IMPORTANT:
  - 画面に映る全要素をここに列挙する。入力項目だけでなく、ボタン・見出し・
    バッジ・メッセージ表示欄など「出力」される要素も全てここに含める。
  - 項目単位の見た目・制約に関する情報は、必ずその項目の行の中に記載する。
    表の外に補足コメントを書いてはならない。
  - 行の順序は画面上の配置順(上から下、左から右)に合わせる。
  - 入力項目には、未入力時に表示されるヒント文言(placeholder)の有無を必ず記載する。
    無い場合も「なし」と明記し、欄を空にしない(記述漏れと区別できるようにする)。
    入力でない項目(ボタン・見出し等)は「—」でよい。
  - 「入出力区分」と「種別」は別の列にする。種別欄に「出力(静的テキスト)」のように
    区分を括弧書きで詰め込まない。同様に、一覧テーブルの行ごとに繰り返される項目
    である、といった種別以外の付帯情報も種別欄に括弧書きせず、備考欄に書く。

  ACTION REQUIRED: 以下は記入例(サンプル)。実際の項目に置き換えること。
-->

| # | 項目名 | 入出力区分 | 種別 | 表示内容・初期値 | プレースホルダ | 制約 | 備考 |
|---|---|---|---|---|---|---|---|
| 1 | [項目名] | [入力 / 表示 / 操作] | [自由入力テキスト / 日付ピッカー / ドロップダウン / 静的テキスト / 条件表示テキスト / バッジ / ボタン / 確認ダイアログ等] | [初期表示時に何が表示されるか] | [ヒント文言、または「なし」。入力でない項目は「—」] | [必須/任意、文字種、桁数、許容値など] | [その項目固有の補足があれば] |

### 処理仕様

<!--
  IMPORTANT:
  - 発火契機(初期表示、またはボタンクリック等の操作)ごとに1行で処理内容を書く。
  - 初期表示は必ず1行目に書く。それ以外の順序は、画面上の操作対象(ボタン等)の
    並び順に合わせることを推奨する。
  - この仕様書はフロントエンドの範囲に閉じる。バックエンドAPIを呼び出す処理が
    ある場合、呼び出すエンドポイント(メソッド+パス。例: `POST /api/todos`)は
    契約として明記する(`openapi/bff/openapi.yaml`と一致させる)。ただし、その
    エンドポイントの詳細な仕様(リクエスト/レスポンスの形、ステータスコード、
    DBへの永続化方法等)はここに再掲せず、`openapi/bff/openapi.yaml`への参照に
    留める。フロントエンド側から見た「成功した場合は〜、失敗した場合は〜」の
    分岐は書く。

  ACTION REQUIRED: 以下はプレースホルダ。実際の処理仕様に置き換えること。
-->

| # | 発火契機 | 処理内容 |
|---|---|---|
| 1 | 初期表示 | [画面が最初に表示される際の処理] |
| 2 | [操作(例: 〜ボタンをクリック)] | [その操作で起きる処理。成功/失敗で分岐する場合はそれぞれ記載] |
