# ユースケース記述: [業務行為のトピック名]

**画面ID**: `[SCREEN_ID]`

**モックアップ**: [Artifact URL]

**作成日**: [DATE]

**関連**: [画面定義書([業務行為のトピック名])](./画面定義書_[画面名].md)

<!--
  ACTION REQUIRED:
  - タイトルは画面名ではなく、この画面が実現する業務行為の名前にすること
    (例: 「Todo一覧画面」ではなく「Todo一覧」、「Todo新規登録画面」ではなく「Todo新規登録」)。
  - ファイル名は `ユースケース記述_<画面名>.md`。<画面名>には連番を付けない
    (画面名そのものが同じ業務内の他画面と重複しないので、番号は不要かつ無意味 — ADR-0015)。
  - 自分がどの画面から起動されるか(遷移元)は書かないこと。自分自身が持つボタン・
    リンクがどこへ導くか(遷移先)は、対になる画面定義書の「処理仕様」に書くこと。
  - 「親機能」のような、この文書がどのディレクトリに属するかの自己言及は書かない
    (ファイルパス自体が既に示している)。
  - 「ステータス: Draft」のような、この文書自体の進捗・ワークフロー状態は書かない
    (設計内容ではなく文書管理の話であり、レビュー状態はPR等で管理する)。
  - 「モックアップ」は、この画面のユースケース記述・画面定義書を起こす元になった
    合意済みモックアップ(designスキルで作成したArtifact)のURLを書く。これは
    openapi.yamlへの参照とは性質が異なるので注意: openapi.yamlは常に現在の契約と
    一致していなければならない「現在の一次情報源」だが、モックアップは執筆時点の
    視覚的合意の記録に過ぎない。ユースケース記述・画面定義書は必ず自己完結させ、
    「詳細はモックアップ参照」のように内容を書かずに済ませることは禁止。この文書の
    記述がモックアップと食い違った場合は常にこの文書が正であり、モックアップを
    都度更新し直す必要はない。モックアップが無い場合は行ごと省略する。
-->

## ユースケース記述

<!--
  IMPORTANT: ここはユーザーストーリー(優先度・独立テスト可否などアジャイル的な情報)を
  書く場所ではない。1つのユースケースにつき、アクター・事前条件・基本フロー・
  代替/例外フロー・事後条件のみを書く。この画面が複数のユースケースを持つ場合は
  UC-01, UC-02 ... を並べる。

  ACTION REQUIRED: 以下はプレースホルダ。実際のユースケースに置き換えること。
-->

### UC-01: [ユースケース名]

**アクター**: [この操作を行う人]

**事前条件**: [この操作を開始できる前提条件]

**基本フロー**:

1. [手順]
2. [手順]

**代替/例外フロー**:

- [条件]: [その場合の結果]

**事後条件**: [完了後の状態]
